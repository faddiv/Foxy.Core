version: '{build}'
image: Visual Studio 2019
configuration: Release

before_build:
- cmd: dotnet restore

artifacts:
  - path: '**\*.nupkg'

deploy:
- provider: NuGet
  api_key:
    secure:
  on:
    appveyor_repo_tag: true
